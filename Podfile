# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MuscleTrainingCounter2 (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MuscleTrainingCounter2 (iOS)
  pod 'Google-Mobile-Ads-SDK'

end

target 'MuscleTrainingCounter2 (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MuscleTrainingCounter2 (macOS)

end
